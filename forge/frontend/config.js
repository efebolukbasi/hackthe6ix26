// Where the Forge backend lives. Empty string = same origin (local dev, where
// the backend serves this frontend). For a split deploy, set e.g.
//   window.FORGE_API = "https://forge-backend.example.com";
window.FORGE_API = "";
